version: "3"

services:

  database:
    image: "postgres:9.6"
    environment:
      - POSTGRES_USER_FILE=/run/secrets/postgres_user
      - POSTGRES_DB=omero
      - POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password
    networks:
      - omero-network
    volumes:
      - "omero-db:/run/lib/postgresql/data"
    secrets:
      - postgres_user
      - postgres_password

  omeroserver:
    image: "openmicroscopy/omero-server:5.6.4"
    links:
      - database:db
    env_file:
      - ./secrets/.env.omeroserver
    networks:
      - omero-network
    ports:
      - 4063:4063
      - 4064:4064
    volumes:
      - "omero-server:/OMERO"

  omeroweb:
    image: "openmicroscopy/omero-web-standalone:master"
    links:
      - omeroserver:omero
    environment:
      - OMEROHOST=omero
    networks:
      - omero-network
    ports:
      - 4080:4080

networks:
  omero-network:

volumes:
  omero-db:
  omero-server:

secrets:
  postgres_user:
    file: ./secrets/postgres_user
  postgres_password:
    file: ./secrets/postgres_password
  omero_root_password:
    file: ./secrets/omero_root_password